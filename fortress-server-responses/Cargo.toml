[package]
name = "fortress-server-responses"
description = "Structured API responses returned by fortress-server"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
